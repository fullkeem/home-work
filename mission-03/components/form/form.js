const emailForm = document.querySelectorAll(".email-form");
const emailLabel = document.querySelectorAll(".email-label");
const emailInput = document.querySelectorAll(".email-input");
const errorMsg = document.querySelectorAll(".error-message");
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
